simple 2d Point class in native js and c++

 - index.js is the entry point for our point module
 - lib/point_native.js is the native implementation in javascript
 - lib/point.js shows c++ bindings extended with native methods
 - src/point.cpp is the clone in c++
 - test.js test script

Compilation:
    $ make

Compile manually:
    node-waf configure
    node-waf build

    binary output in: ./build/Release/

